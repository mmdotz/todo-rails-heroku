<section class='tasks'>
  <h2>Tasks</h2>
  <% all_tasks.each do |task| %>
    <p>
    <a href='/tasks/<%=task.id%>'>
        <%= task.id %> .
        <%= task.task %> --
      <%= task.completed  %>
      </a>
   </p>
  <% end %>
</section>

