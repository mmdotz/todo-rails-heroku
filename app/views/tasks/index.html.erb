<section class='tasks'>
  <h2>Tasks</h2>
  <% all_tasks.each do |task, id| %>
    <p>
        <%= task.id %> .
        <%= task.task %> --
      <%= task.completed  %>
   </p>
  <% end %>
</section>

<!--working!-->
